# OpenClinXR generated environment assets

- `infinigen-ed-exam-bay.glb`
  - SHA-256: `a3b5e68699e3e709a1863b999576ca7c92960daf2059f2313dc51067cfadc5f4`
  - Source: Infinigen Indoors (Princeton VL, BSD-3-Clause) `clinical_bay.gin` seed 0 (`wall_height=2.65`, `aspect_ratio_range=(2.0,2.1)`, no_trim/no_objects), single room post-process extracted from the deterministic multi-room floorplan via mesh-name selection (#236 technique) + Blender glTF export. Room: `dining-room_0`, 6.5 × 6.5 × 2.65 m, floor top at y=0, centered at origin.
  - Purpose: deterministic generated Infinigen clinical-room shell selected by `environmentId` in the UI-XR station environment path (#336).
  - Texture: Cycles DIFFUSE albedo+AO bake (#345, MADR 0055 item 1) via `tools/openclinxr/asset-pipeline/environment/room-albedo-ao-bake.py` — packed `baseColorTexture` on all 3 materials (contact darkening at wall/floor junctions), bake light removed before export. Geometry unchanged (440 tris).
  - Occlusion: native Cycles AO bake (#349) via `tools/openclinxr/asset-pipeline/environment/room-occlusion-bake.py` — separate packed `occlusionTexture` on all 3 materials (native AO bake type, D1). The room is a closed 6.5 m box and Blender 5.1 ignores AO `max_ray_distance`, so the maps are darker (measured mean ~0.04-0.14) than an open room's — the documented closed-room self-occlusion (#345 probe, 3x). Geometry unchanged (440 tris).
  - Boundaries: not production environment readiness, not Quest readiness, not clinical visual validity, not scoring evidence. Footprint/door placement are NOT Infinigen inputs (MADR 0043 / #271): this is one reproducible room, not an exact-dimension generator.
  - External assets used: false (procedural coarse-stage geometry; baked texture is derived from the geometry, no external textures).
- `infinigen-pediatric-urgent-care-bay.glb`
  - SHA-256: `731fed258393fc386a669b626ad78e9b0d42c7652d9230177e19c3226d7eff9e`
  - Source: Infinigen Indoors (Princeton VL, BSD-3-Clause) `clinical_bay.gin` seed 13 (`wall_height=2.65`, `aspect_ratio_range=(2.0,2.1)`, no_trim/no_objects), single room post-process extracted from the deterministic multi-room floorplan via mesh-name selection (#236 technique, `tools/openclinxr/asset-pipeline/environment/infinigen-single-room-extract.py`, `--yaw-deg 90`) + Blender glTF export. Room: `kitchen_0` segment 0, 5.39 × 5.28 m interior, floor top at y=0, centered at origin.
  - **#406 re-bake (2026-08-14):** the #405 bake of this row used `dining-room_0`, whose hull (faces not tagged Visible) coincides with its walls on every horizontal face — an interior room with zero wall thickness — collapsing the derived interior-camera stand-off (`wallThickness=0.000`, black frame in the peds station). Re-baked from seed-1 `hallway_0` (single enclosed segment, at the building's outer edge, real wall thickness 0.12 m) using the same deterministic extract + albedo+AO + occlusion chain. No literal/clamped stand-off: the camera offset is derived from the room's measured thickness (#342). Seeds 3/4 were tried and crash on an upstream Infinigen flake (`material_assignments.utility_floor` lists `ceramic.plaster`/`ceramic.tile` modules, which `room_floors` calls; only floorplans containing a utility room hit it).
  - **#407 re-bake (2026-08-14):** seed-1 `hallway_0` was a 9.9 m corridor (floor aspect 2.02) — the room extraction had taken a hallway, and `deriveInteriorPreviewCamera` pushed the viewpoint ~3.7 m from the cast down the long axis. Re-baked from seed-13 `kitchen_0` (room-shaped: floor 5.28 × 5.39 m, aspect 1.02, 28.5 m²; single enclosed segment) with `--yaw-deg 90` — the extract step's new deterministic orientation flag rotates the room about the up axis so its genuine outer wall (measured +Z hull 0.1093 m) faces the +Z side the interior-camera derivation and the #406 stand-off contract use. Seeds 3/4/5/9/11/12 crash on upstream Infinigen flakes (utility-room material assignment; `Concrete.generate() got an unexpected keyword argument 'vertical'`); seeds 1/2/6/7/8/10/13 complete, and seed 13 is the only one whose room-shaped single-segment room carries a horizontal hull (seed 6's dining-room is a fallback with the same shape class). Geometry is Infinigen's; the orientation is a deterministic transform only (D1). Albedo+AO + occlusion chain unchanged.
  - **black-frame re-bake (2026-08-17):** the #407 bake's exterior mesh carried a full-height interior L-shaped wall fragment (measured 10 faces / 7.95 m² front-facing toward the derived interior eye at `(-2.42, 1.70, 1.90)`) that occluded the entire viewport from inside (0.3% non-black; MADR 0053 measured hide-hull 0.3%→97.4% and forbade it as the product fix). Re-extracted from the same seed-13 `kitchen_0` blend with `--drop-interior-hull-faces` — the extract now drops exterior faces whose world centroid lies strictly inside the wall/floor/ceiling interior volume (2 cm float guard; 26 faces dropped, hull 150→124 tris). The outer shell survives unchanged, so the +Z stand-off (0.1093 m) and the #406 stand-off contract are preserved. Verified at file level after the full chain: 0 front-facing hull faces from the derived eye, interior AABB identical to #407. Albedo+AO + occlusion chain unchanged.
  - Purpose: deterministic generated Infinigen clinical-room shell for `pediatric_urgent_care_bay_v1`, selected by `environmentId` in the UI-XR station environment path (#405, second environmentId after #336; #406 fixed the interior camera stand-off).
  - Texture: Cycles DIFFUSE albedo+AO bake (#345, MADR 0055 item 1) via `tools/openclinxr/asset-pipeline/environment/room-albedo-ao-bake.py` — packed `baseColorTexture` on all 3 materials (contact darkening at wall/floor junctions), bake light removed before export. Geometry unchanged (556 tris).
  - Occlusion: native Cycles AO bake (#349) via `tools/openclinxr/asset-pipeline/environment/room-occlusion-bake.py` — separate packed `occlusionTexture` on all 3 materials. Closed-room self-occlusion documented in the ED bay entry applies identically. Geometry unchanged (556 tris).
  - Boundaries: not production environment readiness, not Quest readiness, not clinical visual validity, not scoring evidence. Footprint/door placement are NOT Infinigen inputs (MADR 0043 / #271): this is one reproducible room, not an exact-dimension generator. The room is a residential-scale hallway; paediatric clinical identity comes from the parametric fixtures the runtime places.
  - External assets used: false (procedural coarse-stage geometry; baked texture is derived from the geometry, no external textures).
- `infinigen-primary-care-clinic.glb`
  - SHA-256: `a76a71eaa660d856c1bccd039e14fe48f3041901b0f9126ecd55a61d9e36fdf8`
  - Source: Infinigen Indoors (Princeton VL, BSD-3-Clause) `clinical_bay.gin` seed 1 (`wall_height=2.65`, `aspect_ratio_range=(2.0,2.1)`, no_trim/no_objects), single room post-process extracted from the deterministic multi-room floorplan via mesh-name selection (#236 technique, `tools/openclinxr/asset-pipeline/environment/infinigen-single-room-extract.py`, `--room bedroom --segment 0` on a segment-pruned copy of the seed-1 blend, `--yaw-deg 0`) + Blender glTF export. Room: `bedroom_0` segment 2, 6.5 × 6.26 m interior, floor top at y=0, centered at origin.
  - Extract-time predicate (2026-08-18, fcd36844): **PASSES** — floorAspect 1.038, floorArea 40.69 m², ceilingHeight 2.41 m, hullFrontFacingToDoorwayEyeCount 0 (≤ 8), doorwayCandidateSurviveCount 5 (≥ 1). The first two candidate tries — seed 6 `dining-room_0` at `--yaw-deg 90` and `--yaw-deg 180` — refused on `hullFrontFacingToDoorwayEyeCount 26 > 8`. Seeds 2/7/8/10 complete but their room-shaped single-segment rooms are interior (no horizontal hull → zero interior-camera stand-off); seed 6's other rooms are corridor-shaped or over/under-sized; seed 13 is the peds bake; seed 0 is the ED bake.
  - Purpose: deterministic generated Infinigen clinical-room shell for `primary_care_clinic_room_v1`, selected by `environmentId` in the UI-XR station environment path (third environmentId after #336/#405; predicate gate fcd36844).
  - Texture: Cycles DIFFUSE albedo+AO bake (#345, MADR 0055 item 1) via `tools/openclinxr/asset-pipeline/environment/room-albedo-ao-bake.py` — packed `baseColorTexture` on all 3 materials (contact darkening at wall/floor junctions), bake light removed before export. Geometry unchanged.
  - Occlusion: native Cycles AO bake (#349) via `tools/openclinxr/asset-pipeline/environment/room-occlusion-bake.py` — separate packed `occlusionTexture` on all 3 materials (measured sd255 16.1–20.8). Closed-room self-occlusion documented in the ED bay entry applies identically. Geometry unchanged.
  - Boundaries: not production environment readiness, not Quest readiness, not clinical visual validity, not scoring evidence. Footprint/door placement are NOT Infinigen inputs (MADR 0043 / #271): this is one reproducible room, not an exact-dimension generator. Clinical identity comes from the parametric fixtures the runtime places.
  - External assets used: false (procedural coarse-stage geometry; baked texture is derived from the geometry, no external textures).
- `infinigen-ed-stroke-bay.glb`
  - SHA-256: `dd30ef77081c535dbe9ed9cb233c6637643c3bf1651327034d34b7d566401fb6`
  - Source: Infinigen Indoors (Princeton VL, BSD-3-Clause) `clinical_bay.gin` seed 2 (`wall_height=2.65`, `aspect_ratio_range=(2.0,2.1)`, no_trim/no_objects), single room post-process extracted from the deterministic multi-room floorplan via mesh-name selection (#236 technique, `tools/openclinxr/asset-pipeline/environment/infinigen-single-room-extract.py`, `--room bedroom --segment 0` on a segment-pruned copy of the seed-2 blend keeping `bedroom_0/1.*`, `--yaw-deg 0`) + Blender glTF export. Room: `bedroom_0` segment 1, 6.89 × 6.77 m interior, floor top at y=0, centered at origin.
  - Extract-time predicate (2026-08-18, fcd36844): **PASSES** — floorAspect 1.017, floorArea 46.66 m², ceilingHeight 2.425 m, hullFrontFacingToDoorwayEyeCount 7 (≤ 8), doorwayCandidateSurviveCount 5 (≥ 1); `--drop-interior-hull-faces` dropped 21 interior-intruding exterior faces (default on). +Z hull thickness measured 0.1126 m on the shipped GLB.
  - Purpose: deterministic generated Infinigen clinical-room shell for `ed_stroke_bay_v1`, selected by `environmentId` in the UI-XR station environment path (fourth environmentId after #336/#405/#421; predicate gate fcd36844).
  - Texture: Cycles DIFFUSE albedo+AO bake (#345, MADR 0055 item 1) via `tools/openclinxr/asset-pipeline/environment/room-albedo-ao-bake.py` — packed `baseColorTexture` on all materials (contact darkening at wall/floor junctions), bake light removed before export. Geometry unchanged.
  - Occlusion: native Cycles AO bake (#349) via `tools/openclinxr/asset-pipeline/environment/room-occlusion-bake.py` — separate packed `occlusionTexture` on all materials (measured sd255 16.6–18.8). Closed-room self-occlusion documented in the ED bay entry applies identically. Geometry unchanged.
  - Boundaries: not production environment readiness, not Quest readiness, not clinical visual validity, not scoring evidence. Footprint/door placement are NOT Infinigen inputs (MADR 0043 / #271): this is one reproducible room, not an exact-dimension generator. Clinical identity comes from the parametric fixtures the runtime places.
  - External assets used: false (procedural coarse-stage geometry; baked texture is derived from the geometry, no external textures).
- `ed-exam-bay-shell.glb`
  - SHA-256: `e390697fea990ea41701fc220224c94b1be2b18ce4cde9778e892261c07124f9`
  - Source: repo-authored local Blender fixture generated by `tools/openclinxr/evidence/environment-artifacts.ts` (build + Cycles albedo+AO bake + occlusion bake as the final emitter stages, #345/#349).
  - Purpose: deterministic ED exam bay shell candidate for local WebXR visual iteration.
  - Boundaries: not production environment readiness, not Quest readiness, not clinical visual validity, not scoring evidence.
  - External assets used: false.
  - 2026-08-12 (#349, MADR 0055 item 1, light half): native Cycles AO bake per material to a SEPARATE packed `occlusionTexture` (glTF Settings "Occlusion" input, exporter convention; base colour untouched) via `tools/openclinxr/asset-pipeline/environment/room-occlusion-bake.py`. 28 of 29 materials wired (measured sd 44.0-127.2 on 0-255); `whiteboard_surface_white` deliberately left without occlusion — it sits 1 mm recessed behind the solid frame's front face, its true AO is uniform, and a flat map fails the luminance-variation gate. Geometry unchanged (792 tris / 67 meshes).
  - 2026-08-12 (#347, MADR 0055 items 4+5): regenerated with parametric trim (skirting, ~2 cm chamfer prisms at wall-wall and wall-floor corners, door reveal frame) and recognisable scale-setting props (duplex outlet plate, light switch, hand-gel dispenser, framed handoff whiteboard, curtain track with rings). 792 triangles / 67 meshes.
  - 2026-08-12 (#345, MADR 0055 item 1): Cycles DIFFUSE albedo+AO bake on the trimmed shell — packed `baseColorTexture` on all 29 materials (contact darkening at wall/floor junctions), bake light removed before export. Geometry unchanged (792 tris / 67 meshes; scene graph diff vs #347's shell identical).
  - 2026-08-12 (#348, MADR 0055 item 3): re-emitted through the merged emitter (build + bake) to add per-surface PBR channel differentiation — vinyl floor rough 0.35 / gypsum wall 0.92 / stainless rail+metal 0.25 metallic 1.0 / glass screen 0.15, light 0.2 / curtain 0.96. Geometry and bake preserved (72 nodes / 67 prims / 792 tris, 29/29 textured); the two metallic-1.0 materials' DIFFUSE bakes go near-black (metals have no diffuse component — physically correct).
- `pediatric_urgent_care_bay_environment.glb`
  - SHA-256: `9c431d8e158cbb7486de557ffaed02e79a0bac9681704b80449ace7dc4af8c62`
  - Source: repo-authored local Blender primitive fixture generated for the public UI-XR runtime path after browser evidence showed the peds bundle referenced a missing environment GLB.
  - Purpose: deterministic pediatric urgent-care bay continuity shell for local UI-XR runtime import and review-safe scene context.
  - Boundaries: not production environment readiness, not Quest readiness, not clinical visual validity, not scoring evidence.
  - External assets used: false.
- `infinigen-adult-ed-abdominal-bay.glb`
  - SHA-256: `54f9172c9b0846a40325e24364f583ab55774912ec1a24b528edd23118edabca`
  - Source: Infinigen Indoors (Princeton VL, BSD-3-Clause) `clinical_bay.gin` seed 0 (`wall_height=2.65`, `aspect_ratio_range=(2.0,2.1)`, no_trim/no_objects), single room post-process extracted from the seed-0 `clinical_bay` blend via mesh-name selection (#236 technique, `tools/openclinxr/asset-pipeline/environment/infinigen-single-room-extract.py`, `--room kitchen --segment 0 --yaw-deg 90`) + Blender glTF export. Room: `kitchen_0` segment 0, 6.25 × 6.25 m floor (aspect 1.0, 39.08 m²), floor top at y=0, centered at origin.
  - Extract-time predicate (2026-08-18, fcd36844): **PASSES** — floorAspect 1.0, floorAreaM2 39.08, ceilingHeightM 2.401, hullFrontFacingToDoorwayEyeCount 7 (≤ 8), doorwayCandidateSurviveCount 5 (≥ 1); `--drop-interior-hull-faces` DEFAULT ON. At yaw 0 the kitchen hull faces −X (measured 0.1245 m), so `--yaw-deg 90` rotates it to the +Z doorway side the interior-camera derivation and #406 stand-off contract use (+Z hull measured 0.1245 m on the shipped GLB). Sweep: all remaining room/segments across the 8 complete blends were extracted with the predicate DEFAULT ON; the first predicate-passing unused room with a horizontal hull is seed-0 `kitchen_0` — the other predicate-passing rooms are INTERIOR (no horizontal hull, so the derived interior-camera stand-off collapses to zero), and the hulled edge rooms are either corridor-shaped or over/under-sized.
  - Purpose: deterministic generated Infinigen clinical-room shell for `adult_ed_abdominal_bay_v1`, selected by `environmentId` in the UI-XR station environment path (fifth environmentId after #336/#405/#421/#423).
  - Texture: Cycles DIFFUSE albedo+AO bake (#345, MADR 0055 item 1) via `tools/openclinxr/asset-pipeline/environment/room-albedo-ao-bake.py` (1024 px) — packed `baseColorTexture` on all 3 materials (contact darkening at wall/floor junctions), bake light removed before export. Geometry unchanged (475 tris).
  - Occlusion: native Cycles AO bake (#349) via `tools/openclinxr/asset-pipeline/environment/room-occlusion-bake.py` (512 px) — separate packed `occlusionTexture` on all 3 materials (measured sd255 19.2–28.1). Closed-room self-occlusion documented in the ED bay entry applies identically. Geometry unchanged (475 tris).
  - Boundaries: not production environment readiness, not Quest readiness, not clinical visual validity, not scoring evidence. Footprint/door placement are NOT Infinigen inputs (MADR 0043 / #271): this is one reproducible room, not an exact-dimension generator. Clinical identity comes from the parametric fixtures the runtime places.
  - External assets used: false (procedural coarse-stage geometry; baked texture is derived from the geometry, no external textures).
- `infinigen-telehealth-home-visit.glb`
  - SHA-256: `476a8e402b2dc8c4f2023536f225325a16006828cd4e865880311923a811aa01`
  - Source: Infinigen Indoors (Princeton VL, BSD-3-Clause) `clinical_bay.gin` seed 14 (`wall_height=2.65`, `aspect_ratio_range=(2.0,2.1)`, no_trim/no_objects), single room post-process extracted via mesh-name selection (#236 technique, `tools/openclinxr/asset-pipeline/environment/infinigen-single-room-extract.py`, `--room bedroom --segment 0 --yaw-deg 180`) from a segment-pruned copy of the seed-14 blend keeping `bedroom_0/2.*` + Blender glTF export. Room: `bedroom_0` segment 2, 9.5 × 6.38 m floor (aspect 1.518, 59.44 m²), floor top at y=0, centered at origin. `--drop-interior-hull-faces` DEFAULT ON (0 interior-intruding exterior faces dropped).
  - Extract-time predicate (2026-08-18): **PASSES** — floorAspect 1.518, floorAreaM2 59.44, ceilingHeightM 2.407, hullFrontFacingToDoorwayEyeCount 0 (≤ 8), doorwayCandidateSurviveCount 5 (≥ 1). Seed 14 was generated fresh (71 s) because the 8 complete on-disk blends were exhausted for unused hulled rooms; seed 14's room-shaped single-segment rooms are interior (no horizontal hull), and the only predicate-passing room with a horizontal hull is `bedroom_0` segment 2. At yaw 0 its hull faces −Z, so `--yaw-deg 180` rotates it to the +Z doorway side the interior-camera derivation and #406 stand-off contract use (+Z hull measured 0.1214 m on the shipped GLB). Geometry is Infinigen's; the orientation is a deterministic transform only (D1).
  - Purpose: deterministic generated Infinigen clinical-room shell for `telehealth_home_visit_v1`, selected by `environmentId` in the UI-XR station environment path (sixth environmentId after #336/#405/#421/#423/#424).
  - Texture: Cycles DIFFUSE albedo+AO bake (#345, MADR 0055 item 1) via `tools/openclinxr/asset-pipeline/environment/room-albedo-ao-bake.py` (1024 px) — packed `baseColorTexture` on all 3 materials (contact darkening at wall/floor junctions), bake light removed before export. Geometry unchanged (496 tris).
  - Occlusion: native Cycles AO bake (#349) via `tools/openclinxr/asset-pipeline/environment/room-occlusion-bake.py` (512 px) — separate packed `occlusionTexture` on all 3 materials (measured sd255 15.1–19.6). Closed-room self-occlusion documented in the ED bay entry applies identically. Geometry unchanged (496 tris).
  - Boundaries: not production environment readiness, not Quest readiness, not clinical visual validity, not scoring evidence. Footprint/door placement are NOT Infinigen inputs (MADR 0043 / #271): this is one reproducible room, not an exact-dimension generator. Clinical identity comes from the parametric fixtures the runtime places.
  - External assets used: false (procedural coarse-stage geometry; baked texture is derived from the geometry, no external textures).
- `infinigen-behavioral-health-private.glb`
  - SHA-256: `5cf3695848ee6a53abdaf9b6aa4439590569a87c1f1dd424d9a685eab0cf7afa`
  - Source: Infinigen Indoors (Princeton VL, BSD-3-Clause) `clinical_bay.gin` seed 16 (`wall_height=2.65`, `aspect_ratio_range=(2.0,2.1)`, no_trim/no_objects), single room post-process extracted via mesh-name selection (#236 technique, `tools/openclinxr/asset-pipeline/environment/infinigen-single-room-extract.py`, `--room dining-room --segment 0 --yaw-deg 90`) + Blender glTF export. Room: `dining-room_0` segment 0, 5.0 × 5.88 m interior (aspect 1.18, 28.12 m²), floor top at y=0, centered at origin. `--drop-interior-hull-faces` DEFAULT ON (0 interior-intruding exterior faces dropped).
  - Extract-time predicate (2026-08-18): **PASSES** — floorAspect 1.18, floorAreaM2 28.12, ceilingHeightM 2.412, hullFrontFacingToDoorwayEyeCount 0 (≤ 8), doorwayCandidateSurviveCount 5 (≥ 1). Seed 15 crashes on the upstream Infinigen flake (`Concrete.generate() got an unexpected keyword argument 'vertical'`, same class as seeds 3/4/5/9/11/12); seed 16 completes. At yaw 0 `dining-room_0` is INTERIOR (no horizontal hull on any face); its −X hull (measured 0.1192 m) is rotated to the +Z doorway side by `--yaw-deg 90` (+Z hull measured 0.1192 m on the shipped GLB). Geometry is Infinigen's; the orientation is a deterministic transform only (D1).
  - Purpose: deterministic generated Infinigen clinical-room shell for `behavioral_health_private_room_v1`, selected by `environmentId` in the UI-XR station environment path (seventh environmentId after #336/#405/#421/#423/#424).
  - Texture: Cycles DIFFUSE albedo+AO bake (#345, MADR 0055 item 1) via `tools/openclinxr/asset-pipeline/environment/room-albedo-ao-bake.py` (1024 px) — packed `baseColorTexture` on all 3 materials (contact darkening at wall/floor junctions), bake light removed before export. Geometry unchanged (456 tris).
  - Occlusion: native Cycles AO bake (#349) via `tools/openclinxr/asset-pipeline/environment/room-occlusion-bake.py` (512 px) — separate packed `occlusionTexture` on all 3 materials (measured sd255 36.8–40.0). Closed-room self-occlusion documented in the ED bay entry applies identically. Geometry unchanged (456 tris).
  - Boundaries: not production environment readiness, not Quest readiness, not clinical visual validity, not scoring evidence. Footprint/door placement are NOT Infinigen inputs (MADR 0043 / #271): this is one reproducible room, not an exact-dimension generator. Clinical identity comes from the parametric fixtures the runtime places.
  - External assets used: false (procedural coarse-stage geometry; baked texture is derived from the geometry, no external textures).
- `infinigen-oncology-consult.glb`
  - SHA-256: `65969fe479bcb6e07e7a1ffdbd314cfb3be0449a8199edc9e79549e33ec9415b`
  - Source: Infinigen Indoors (Princeton VL, BSD-3-Clause) `clinical_bay.gin` seed 17 (`wall_height=2.65`, `aspect_ratio_range=(2.0,2.1)`, no_trim/no_objects), single room post-process extracted via mesh-name selection (#236 technique, `tools/openclinxr/asset-pipeline/environment/infinigen-single-room-extract.py`, `--room dining-room --segment 0 --yaw-deg 0`) + Blender glTF export. Room: `dining-room_0` segment 0, 5.5 × 5.26 m floor (aspect 1.046, 28.92 m²), floor top at y=0, centered at origin. `--drop-interior-hull-faces` DEFAULT ON (0 interior-intruding exterior faces dropped).
  - Extract-time predicate (2026-08-18): **PASSES** — floorAspect 1.046, floorAreaM2 28.92, ceilingHeightM 2.409, hullFrontFacingToDoorwayEyeCount 0 (≤ 8), doorwayCandidateSurviveCount 5 (≥ 1). Seed 17 was generated fresh (coarse, `compose_indoors.terrain_enabled=False`); the 8 complete on-disk blends were exhausted for unused hulled rooms. Seed 17's room-shaped single-segment rooms with a horizontal hull are `dining-room_0` (hull on −Y, aspect 1.046, 28.92 m²), `garage_0` (hull on −Y, aspect 1.021, 35.28 m²) and `bathroom_0` seg 0 (hull on +X/+Y, aspect 1.133, 16.01 m²). At yaw 0 `dining-room_0`'s −Y hull (measured 0.1206 m) maps to the +Z doorway side the interior-camera derivation and #406 stand-off contract use (+Z hull measured 0.1206 m on the shipped GLB). Geometry is Infinigen's; the orientation is a deterministic transform only (D1).
  - Purpose: deterministic generated Infinigen clinical-room shell for `oncology_consult_room_v1`, selected by `environmentId` in the UI-XR station environment path (eighth environmentId after #336/#405/#421/#423/#424).
  - Texture: Cycles DIFFUSE albedo+AO bake (#345, MADR 0055 item 1) via `tools/openclinxr/asset-pipeline/environment/room-albedo-ao-bake.py` (1024 px) — packed `baseColorTexture` on all 3 materials (contact darkening at wall/floor junctions), bake light removed before export. Geometry unchanged (464 tris).
  - Occlusion: native Cycles AO bake (#349) via `tools/openclinxr/asset-pipeline/environment/room-occlusion-bake.py` (512 px) — separate packed `occlusionTexture` on all 3 materials (measured sd255 23.3–34.4). Closed-room self-occlusion documented in the ED bay entry applies identically. Geometry unchanged (464 tris).
  - Boundaries: not production environment readiness, not Quest readiness, not clinical visual validity, not scoring evidence. Footprint/door placement are NOT Infinigen inputs (MADR 0043 / #271): this is one reproducible room, not an exact-dimension generator. Clinical identity comes from the parametric fixtures the runtime places.
  - External assets used: false (procedural coarse-stage geometry; baked texture is derived from the geometry, no external textures).
- `infinigen-urgent-care-clinic.glb`
  - SHA-256: `5aa1c6f1f086109f23e736839cb9130660955f22bf9394fd58f9b01ea412c7a6`
  - Source: Infinigen Indoors (Princeton VL, BSD-3-Clause) `clinical_bay.gin` seed 22 (`wall_height=2.65`, `aspect_ratio_range=(2.0,2.1)`, no_trim/no_objects), single room post-process extracted via mesh-name selection (#236 technique, `tools/openclinxr/asset-pipeline/environment/infinigen-single-room-extract.py`, `--room bedroom --segment 0 --yaw-deg 270`) from a segment-pruned copy of the seed-22 blend keeping `bedroom_0/0.*` wall/floor/ceiling/exterior + Blender glTF export. Room: `bedroom_0` segment 0, 7.5 × 8.0 m floor (aspect 1.052, 57.32 m²), floor top at y=0, centered at origin. `--drop-interior-hull-faces` DEFAULT ON (57 interior-intruding exterior faces dropped).
  - Extract-time predicate (2026-08-18): **PASSES** — floorAspect 1.052, floorAreaM2 57.32, ceilingHeightM 2.427, hullFrontFacingToDoorwayEyeCount 0 (≤ 8), doorwayCandidateSurviveCount 4 (≥ 1). At yaw 270 `bedroom_0/0`'s hull maps to the +Z doorway side the interior-camera derivation and #406 stand-off contract use (+Z hull measured 0.1179 m on the shipped GLB). Geometry is Infinigen's; the orientation is a deterministic transform only (D1).
  - Doorway-camera re-bake (2026-08-18): the first bake of this row used seed 18 `bedroom_0/1` yaw 180 (sha `d4a0c060…`); its +Z doorway side was a SOLID interior partition — all five doorway camera candidates' look-rays hit `bedroom_0/1.wall` before the actor centre, so the capture photographed a wall close-up (parent rejected). Dump: `.openclinxr/evidence/rooms-urgent-care-clinic/camera-candidates.json` (surviving 0 / rejected 5). Seeds 19 (no predicate-passing hulled room), 20 (`room_floors` module-not-callable), 21 (`Concrete.generate(vertical)`) were tried and skipped. Seed 22 `bedroom_0/0` yaw 270 is the first clean candidate: 4 of 5 doorway candidates survive the look-ray test to the live actor centre (only the +X corner is blocked by `bedroom_0/0.wall` at 0.35 m), so the derived camera looks into the room interior rather than at a wall face.
  - Purpose: deterministic generated Infinigen clinical-room shell for `urgent_care_clinic_room_v1`, selected by `environmentId` in the UI-XR station environment path (ninth environmentId after #336/#405/#421/#423/#424).
  - Texture: Cycles DIFFUSE albedo+AO bake (#345, MADR 0055 item 1) via `tools/openclinxr/asset-pipeline/environment/room-albedo-ao-bake.py` (1024 px) — packed `baseColorTexture` on all materials (contact darkening at wall/floor junctions), bake light removed before export. Geometry unchanged (745 tris).
  - Occlusion: native Cycles AO bake (#349) via `tools/openclinxr/asset-pipeline/environment/room-occlusion-bake.py` (512 px) — separate packed `occlusionTexture` on all materials (measured sd255 12.4–14.5). Closed-room self-occlusion documented in the ED bay entry applies identically. Geometry unchanged (745 tris).
  - Boundaries: not production environment readiness, not Quest readiness, not clinical visual validity, not scoring evidence. Footprint/door placement are NOT Infinigen inputs (MADR 0043 / #271): this is one reproducible room, not an exact-dimension generator. Clinical identity comes from the parametric fixtures the runtime places.
  - External assets used: false (procedural coarse-stage geometry; baked texture is derived from the geometry, no external textures).

