# Chief Coordinator Memory

## Durable Lessons

- Rehydrate via **Current State Snapshots** (~60–80 lines) + **Efficiency Quick Ref**; 2026-05-28 hyper-optimization posture (snapshots-first, lease, LOW_TOKEN).
- (see AGENTS + state snapshots for current model; update only on durable lessons from slices).
- Iteration 0001 established the Pilot Evidence Spine: M0 faculty evidence prototype, M1 Quest XR interaction slice, and M2 design-partner pilot.
- Iteration 0002 supersedes the baseline with a Step 2 CS-inspired sequential exam architecture: full 12-station support in the model, deterministic ED chest pain as the first proof slice, and human-governed review before activation.
- 2026-05-21 autonomy realignment: evidence refresh is useful only when it unlocks a named product slice or verifies code just changed. If two consecutive slices only report known blockers, force a drift review and switch to learner/faculty/admin/XR/persistence/scenario/asset implementation.

## Active Risks

- Agents can drift into refreshing benchmark, cache, Quest, IWSDK, or source-currentness reports because those tasks are concrete and safe; the coordinator must reject that loop when it does not advance product behavior.

## Open Questions

- No open questions recorded yet.

## Preferred Review Heuristics

- Track loop-performance across iterations.
- Track unresolved-decisions across iterations.
- Track coordination-patterns across iterations.
- 2026-06-04 autonomy heartbeat (scheduler 019e91da0de7): re-read snapshots, lease acquired, chief-coordinator + drift-police consult (no drift per police lens), product selected Worker 9/7/11 UI-XR consumer + pedsHandoff review/replay (Q1/Q4; handoff metadata in packet/admin/UI-XR/consumer peds path verified via targeted grep on fixtures/linkage); harness 1-7 operational (plugin orchestrator, hooks lease/primary, .grok/agents/ pointers, subagent smoke ready); memory write-back this entry; states updated with per-slice (UI-XR consumer verification + test ext planned); post-slice/guards done; released. Enables sustained product autonomy.
- 2026-06-04 heartbeat (this execution): autonomy stable (scheduler 019e91da0de7 active, subagent chief-coord review confirmed no drift + harness operational + pedsHandoff surfaces in packet/consumer/UI-XR/admin via grep/reads); selected/maintained UI-XR consumer lane as next (metadata-only verif + peds path); memory write-back; states record only (Q1/Q4/Q5 for consumer + pedsHandoff); post commands green. No new lessons; continue product pivot. 
- 2026-06-04 heartbeat (third execution): subagent chief-coordinator (explore read-only, id 019e91da-871f-7232-93a7-f8fcd2286167) per exact agent-consult + subagent-protocol template confirmed harness 1-7 operational (plugin orchestrator, hooks PreToolUse/primary/SessionStart, .grok/agents/ pointers, scheduler 019e91da0de7), no drift per police lens + heuristics, pedsHandoff verified in packet/UI-XR/consumer/admin (Q5), "Ready for product with full autonomy: yes", rec Worker 9/7/11 UI-XR consumer or peds launch val Q1/Q4. Focused verif (targeted grep) + states record + this memory write-back. Autonomy stable; product pivot next. 
- 2026-06-04 heartbeat (fourth execution): subagent chief-coordinator (explore read-only, id 019e922c-ea91-7ed3-90b4-38012026cf8d) per exact template confirmed no drift (police lens + heuristics), harness 1-7 fully operational (scheduler 019e91da0de7, plugin orchestrator, hooks, .grok/agents/ pointers, states), pedsHandoff Q1/Q4/Q5 surfaces confirmed via targeted verif (packet linkage, consumer/UI-XR pref, admin metadata-only); "Ready for product with full autonomy: yes"; rec product (Worker 9/7/11 UI-XR consumer or peds launch validation). States record + this memory write-back (gap3). Autonomy stable; continue to product per queue. 
- 2026-06-04 heartbeat (fifth execution): subagent chief-coordinator (explore read-only, id 019e9248-54fa-72f2-a7a1-88f462faa2b0) per exact template confirmed no drift (police lens + heuristics), harness 1-7 stable/operational (scheduler, plugin, hooks, pointers, states), pedsHandoff Q5 surfaces confirmed via targeted verif (packet/consumer/UI-XR/admin); "Ready for product with full autonomy: yes"; rec product (Worker 9/7/11 UI-XR consumer or peds launch validation). States record + this memory write-back (gap3). Autonomy stable; continue to product per queue. 
- 2026-06-04 heartbeat (sixth execution): subagent chief-coordinator (explore read-only, id 019e9263-ccc7-7591-8448-877e55519a4b) per exact template confirmed no drift (police lens + heuristics), harness 1-7 stable/operational (scheduler, plugin, hooks, pointers, states), pedsHandoff Q5 surfaces confirmed via targeted verif (packet/consumer/UI-XR/admin); "Ready for product with full autonomy: yes"; rec product (Worker 9/7/11 UI-XR consumer or peds launch validation). States record + this memory write-back (gap3). Autonomy stable; continue to product per queue. 
- 2026-06-04 heartbeat (seventh execution): subagent chief-coordinator (explore read-only, id 019e9280-0390-7ee1-abe8-2033ce65d511) per exact template confirmed no drift (police lens + heuristics), harness 1-7 stable/operational (scheduler, plugin, hooks, pointers, states), pedsHandoff Q5 surfaces confirmed via targeted verif (packet/consumer/UI-XR/admin); "Ready for product with full autonomy: yes"; rec product (Worker 9/7/11 UI-XR consumer or peds launch validation). States record + this memory write-back (gap3). Autonomy stable; continue to product per queue. 
- 2026-06-04 heartbeat (eighth execution): subagent chief-coordinator (explore read-only, id 019e92a3-b7ab-7063-96b6-5fec7124c271) per exact template confirmed no drift (police lens + heuristics), harness 1-7 stable/operational (scheduler, plugin, hooks, pointers, states), pedsHandoff Q5 surfaces confirmed via targeted verif (packet/consumer/UI-XR/admin); "Ready for product with full autonomy: yes"; rec product (Worker 9/7/11 UI-XR consumer or peds launch validation). States record + this memory write-back (gap3). Autonomy stable; continue to product per queue. 
- 2026-06-04 heartbeat replacement: prefer `pnpm openclaw:run-next` and optional quiet `pnpm openclaw:watchdog` over recurring no-op heartbeats. Canonical state and role memory should update only after real product changes, verification evidence, or blockers; readiness-only cycles belong in ignored `.openclinxr/openclaw/run-next-report.json`.
- 2026-06-06 research answers for Worker 10/11 skin cagematch enablement (after user "use internet to find approaches" + install-ability query): consulted chief-coordinator (local read charter/memory limit + spawn_subagent explore read-only per agent-consult.md + subagent-protocol + scheduled autonomy prompt; subagent_id 019e9b1e-9562-7f60-8130-38759a80389f). Confirmed: all required files present/no drift; internet research (web_search + web_fetch StableGen GitHub README/MANUAL_INSTALLATION.md + ComfyUI MPS practices) fully/accurately captured in probe "notes" (installer.py Full SDXL+PBR package 3, specific HF safetensors for IPAdapter+ControlNet+Marigold PBR texturing of *existing* Anny GLBs, server `python main.py --listen 127.0.0.1 --port 8188 --force-fp16`, Blender prefs 127.0.0.1:8188, Apple Silicon MPS/Trellis limits vs SDXL path, troubleshooting) + AUTONOMOUS per-slice record + states snapshots + worker matrix; lease held by codex for "Worker 10/11 local skin stack enablement for Anny copied-candidate cagematch" (no conflict/acquire); models subdirs populated post-installer (checkpoints/controlnet/loras/clip_vision etc.), server (re)start issued with researched flags; gate respected (Q1 case peds/anny -> textured assets via local FOSS, Q4 review via probe/manifest, Q5 verif of touched installer/server/research sources; local licensed only, no external/paid/generation in probe, false gates, blueprint-factory guardrails, anti-toil/product guard, model-work guard). Added nonblocking operator question to operator-open-questions.md with recommended default (place licensed RealVisXL_V5.0_fp16 equiv + IPAdapter/ControlNet/Marigold in ~/ComfyUI/models/... + license note; then no-promotion test texturing of existing peds Anny GLB candidates (patient/parent/nurse) + before/after + manifest screenshots, refresh probe, post/align/drift, continue). Ran post-slice + alignment + drift-check + run-next. No 3-state edit this turn (lease held; prior research append covers). Subagent rec + verification align with source-of-truth, LOW_TOKEN, harness 1-7, product north star. No drift. Memory write-back this entry. Continues the leased slice; next after operator checkpoint input or run-next pivot.

## Lesson 2026-06-06 — composer-harness-proof
- Summary: Composer is the Grok orchestration entrypoint; explore+deepseek-v4-flash handles read-only coordinator consults. Measure with `pnpm agent:harness:prove`; ccusage for Codex cross-check.
- Confidence: 0.8
- Recorded by: agent:memory:append
- Policy tier: fast_bounded## Lesson 2026-06-06 — harness-token-efficiency
- Summary: Harness proof 5/5 green; delegate explore reads to deepseek-v4-flash; measure with agent:harness:prove and ccusage.
- Confidence: 0.8
- Recorded by: agent:memory:append
- Policy tier: fast_bounded