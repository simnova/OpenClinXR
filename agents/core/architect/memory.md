# Architect memory

## Lessons

- Wave C (2026-08-02) BOD APPROVED C-arch: composition is **hard law**, not optional style. Features → packages; apps = shells; tools = CLIs.
- Delivery must be **role-mapped** to `pathScope.writeRoots` owners. Silent multi-root general-purpose is a major SoD defect.
- Distinction: **architect** = composition/layering; **xr-systems-architect** = UI-XR/arena runtime IC; **hrbp** = roster + PATH-SCOPE SSOT.
- Co-own COMPOSITION-ROOTS with hrbp (SHARED). Do not dual-SSOT path globs in this charter — policy owns machine roots.
- Worktree promote (C-worktree) is implementer CLI residual; isolation spawn alone is not a full lifecycle.

## Standing heuristics

1. New feature? Prefer `packages/openclinxr/<domain>` over `apps/**` dump.
2. App change? Confirm it is host bind/wiring under an app-owning role's writeRoots.
3. Multi-root slice? Require multi-role team-spawn, not one unmapped GP agent.
4. On layer violation: cite path + owning role + COMPOSITION-ROOTS / PATH-SCOPE §Wave C.
