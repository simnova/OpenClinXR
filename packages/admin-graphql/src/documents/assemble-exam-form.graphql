mutation AssembleExamForm($input: AssembleExamFormInput!) {
  assembleExamForm(input: $input) {
    examFormId
    blueprintId
    status
    stationRefs {
      order
      scenarioId
      scenarioVersion
      title
    }
  }
}
