{
  "name": "@openclinxr/scenario-runtime",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "lint": "biome lint --no-errors-on-unmatched .",
    "typecheck": "tsgo --noEmit -p tsconfig.vitest.json",
    "test": "vitest run --root .",
    "build": "tsgo --build",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@openclinxr/asset-registry": "workspace:*",
    "@openclinxr/capability-gateway": "workspace:*",
    "@openclinxr/conversation-policy": "workspace:*",
    "@openclinxr/domain": "workspace:*",
    "@openclinxr/exam-assembly": "workspace:*",
    "@openclinxr/model-gateway": "workspace:*",
    "@openclinxr/review-workflow": "workspace:*",
    "@openclinxr/scenario-fixtures": "workspace:*",
    "@openclinxr/session-state": "workspace:*",
    "@openclinxr/shared-schemas": "workspace:*",
    "@cellix/trace-ledger": "workspace:*",
    "@openclinxr/voice-gateway": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "catalog:",
    "@cellix/config-vitest": "workspace:*",
    "@cellix/config-typescript": "workspace:*"
  },
  "files": [
    "dist"
  ]
}
