/**
 * Compat re-export. Catalog + bakers live in `@openclinxr/factory-stations`.
 * Admin cards may keep this import for one cycle.
 */
export {
  PRODUCTION_STATION_IDS,
  factoryStationSchemas,
  productionStationIds,
  type FactoryStationSchema,
  type ProductionStationId,
  type StandardIssue,
  type StandardResult,
  type StationJsonSchema,
  type StationPropertySchema,
} from "@openclinxr/factory-stations";
