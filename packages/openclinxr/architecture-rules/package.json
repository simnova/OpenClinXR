{
  "name": "@openclinxr/architecture-rules",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "architecture": "vitest run --root .",
    "lint": "biome check --no-errors-on-unmatched .",
    "typecheck": "tsgo --noEmit -p tsconfig.json",
    "test": "vitest run --root ."
  },
  "dependencies": {
    "@openclinxr/capability-gateway": "workspace:*",
    "@openclinxr/domain": "workspace:*",
    "archunit": "2.3.0"
  },
  "devDependencies": {
    "typescript": "6.0.3",
    "vitest": "4.1.5"
  }
}
