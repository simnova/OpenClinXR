{
  "name": "@openclinxr/architecture-rules",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "architecture": "vitest run --config vitest.arch.config.ts --root .",
    "lint": "biome check --no-errors-on-unmatched .",
    "typecheck": "tsgo --noEmit -p tsconfig.json",
    "test": "vitest run --root . --passWithNoTests"
  },
  "dependencies": {
    "@openclinxr/capability-gateway": "workspace:*",
    "@openclinxr/domain": "workspace:*",
    "archunit": "2.3.0"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "catalog:",
    "@cellix/config-vitest": "workspace:*",
    "@cellix/config-typescript": "workspace:*"
  }
}
