# OpenClinXR Packages

This folder owns OpenClinXR-specific workspace packages.

Package names remain `@openclinxr/*`; this folder only separates project-owned code from shared `packages/cellix/*` library code.
