{
  "name": "@openclinxr/capability-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "lint": "biome lint --no-errors-on-unmatched .",
    "typecheck": "tsgo --noEmit -p tsconfig.vitest.json",
    "test": "vitest run --root .",
    "build": "tsgo --build",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@azure/storage-queue": "^12.29.0",
    "@cellix/provider-contracts": "workspace:*",
    "@openclinxr/motion-compiler": "workspace:*",
    "@openclinxr/scenario-fixtures": "workspace:*"
  },
  "devDependencies": {
    "@cellix/config-typescript": "workspace:*",
    "@cellix/config-vitest": "workspace:*",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "files": [
    "dist"
  ]
}
