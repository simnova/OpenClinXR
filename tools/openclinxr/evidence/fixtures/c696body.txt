Factory: Landed (wt/issue-696 @ 49eedbd2)

## IN-SCOPE

- `tools/openclinxr/openclaw/dispatch-worker.ts` now writes `backgroundJobsAliveAtExit` on every terminal ledger row: the count of the worker's descendant processes still alive when the worker's own process exited — sampled from the process table (`ps -axo pid=,ppid=`, 1s interval + an exit-time sample) while the child ran, re-checked after `close`. Harness-observed; never read from the worker's report.
- A completed child that left >= 1 live background process is recorded `phase: "reaped"` (new union member) instead of `completed`. `factory-pulse` and `campaign-track` select `phase === "completed"`, so a reaped row is no longer counted as a completion. A child that died without an end event stays `died`.
- Planted RED clauses (1) and (2) flipped `it.fails` -> `it`; `## FIXED (#696)` block appended; header and assertions otherwise untouched.
- Shared ledger annotated in place: issue-693 -> `phase: "reaped"`, `backgroundJobsAliveAtExit: 1`; issue-700 -> `backgroundJobsAliveAtExit: 0`, stays `completed` (known-good column). All other rows byte-identical; backup at `/tmp/worker-sessions-backup-696.jsonl`.
- Verified: `run:` 5/5 pass in the worktree; `live:` 0 `it.fails(` clauses remain; `pnpm packages:typecheck:agent` 67/67; mechanism smoke-tested against a real backgrounded child (a `sleep 45` launched by a short-lived child is observed as a survivor; a short-lived foreground child is correctly excluded).

**Does this PREVENT the reap, or only RECORD it? — RECORD only.** The reap is unchanged: `dispatch-worker.ts` spawns the worker with no `detached` option (line ~1603), so the worker's background jobs share the dispatch's process group and are still killed when the dispatch process exits. My change only makes the ledger say `phase: "reaped"` instead of `completed` for such a dispatch. A multi-hour background bake dispatched today would STILL be reaped at dispatch exit — #697's four-arm conditioning run and #698's fleet re-creation cannot be dispatched expecting their bakes to survive on this fix. What this card buys is that a reaped dispatch is no longer recorded as a clean completion: the ledger tells the truth, so the orchestrator knows to resume rather than believing the work landed. The lane is usable in the sense that a reaped dispatch is visible as reaped — not in the sense that bakes survive.

**Proofs that could not pass as written:** the `run:` and `live:` proofs reference the planted test whose `LEDGER` path was `process.cwd()`-relative — and a worker worktree has no `.openclinxr/openclaw` (gitignored by design), so the test could never find the ledger in the worktree where the proof runs. I fixed the path to resolve through the shared coordination root (`resolveSharedCoordinationPath`, git common dir -> main checkout); the assertions are unchanged. The `changed:` proof target (`tools/openclinxr/openclaw/dispatch-worker.ts`) sits outside my charter's listed write roots but is exactly the file the card requires — edited per the contract. No proof passed trivially: clause (1) asserts a real field on the real known-good row, clause (2) compares phase/stopReason on two real rows, and counterweights (3)-(5) pin the known-good column and the surviving terminal fields.

## OUT-OF-SCOPE (seen, not fixing)

- `pnpm architecture` reds in this worktree: markdown-references reports PROJECT_STATUS.md's 4 unresolved refs (`.openclinxr/handoffs/*`, `.openclinxr/slices/*/STATE.md` — gitignored, absent by design in a worktree) and doc-authority-registry's 1 ref to `apps/arena/model-vetting-studio/dist/assets/AGENTS-BYKezdFc.md` (build artifact absent here). `wiki/index.md` does not resolve even on main. I touched no Markdown.
- openclaw suite (worktree-absent gitignored files): `dispatch-resolves-the-model-from-the-role-policy` (3) ENOENT `.openclinxr/openclaw/superagent-loop-prompt.md`; `the-ready-depth-floor-is-derived-not-picked` ENOENT `supervisor-audit-latest.json`; `the-supervisor-reports-what-is-not-self-correcting` (14)(16) read gitignored contract-verify artifacts. `only-one-integrator-mutates-main-at-a-time` (5) is a flaky concurrency race — fails 2 of 3 runs with my changes stashed.
- `pnpm typecheck:relaxed` carries 35 pre-existing errors in untouched files; zero in my two files.
- The worker-side behaviour itself: a dispatched worker that backgrounds a job still loses it at dispatch exit (see the RECORD-only answer above).

## CLAIM

The ledger now carries a harness-observed background-job signal (`backgroundJobsAliveAtExit`) and records a reaped dispatch as `phase: "reaped"` rather than `completed`, so issue-693 and issue-700 are mechanically distinguishable without reading `handoff`, and the planted test passes 5/5 in the worktree. The change RECORDS the reap; it does not prevent it.

## NOT TESTED

Whether detaching the worker's background job is the right remedy (may break the worktree-scoped write deny — the card's stated residual; not decided here); whether the reap comes from process-group membership vs the harness terminating the process tree (no measurement of either); whether the 1s sampling gap misses a job launched in the final <1s before worker exit; `ps` output-shape portability beyond this machine.

