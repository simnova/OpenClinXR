{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "search_replace|write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "if [ \"${OPENCLINXR_PHASE_GUARD:-0}\" != \"1\" ]; then exit 0; fi; if [ \"${OPENCLINXR_WORKER:-0}\" = \"1\" ] || [ \"${OPENCLINXR_WORKER:-}\" = \"true\" ] || [ -n \"${GROK_SUBAGENT:-}\" ]; then exit 0; fi; npx tsx ../../tools/openclinxr/openclaw/agent-phase-guard.ts",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
