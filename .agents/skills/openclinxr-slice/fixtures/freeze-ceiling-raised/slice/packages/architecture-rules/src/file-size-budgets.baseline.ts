/**
 * Baseline SIZE_FREEZE snapshot (pre-slice).
 */
export const SIZE_FREEZE: Record<string, { maxLines: number; reason: string }> = {
	'apps/ui-xr/src/main.ts': {
		maxLines: 10255,
		reason: 'XR runtime god-file — split by subsystem',
	},
};
