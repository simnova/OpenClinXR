# Iterations (cold pointer)

**Why this README survives:** the live `iterations/` tree is intentionally empty of iteration dumps after the 2026-08-05 status-doc purge. This file is the **only** hot-path entry so agents do not invent a second SSOT or recreate dead stubs. Delete it only if a living process doc fully owns the same pointer (today: operating-loop + warehouse wiki).

Agent-factory iteration **status dumps** were purged 2026-08-05 (git history retains full text). Catalog: [`docs/_archive/iterations/0009/ARCHIVE-MANIFEST.json`](../docs/_archive/iterations/0009/ARCHIVE-MANIFEST.json).

**Retained cold body** (alignment + durable synthesis lesson only — **must keep resolving**):

- [`docs/_archive/iterations/0009/07-final-synthesis.md`](../docs/_archive/iterations/0009/07-final-synthesis.md)

Wiki note: [`docs/_archive/wiki/topics/agent-factory-iterations.md`](../docs/_archive/wiki/topics/agent-factory-iterations.md).

Living process: [`docs/agent-factory/operating-loop.md`](../docs/agent-factory/operating-loop.md) · [`docs/agent-factory/README.md`](../docs/agent-factory/README.md).

Do **not** recreate hot stubs under `iterations/iteration-0009/`. When a full factory loop is required, create a **new** `iterations/iteration-XXXX/` directory per operating-loop.
