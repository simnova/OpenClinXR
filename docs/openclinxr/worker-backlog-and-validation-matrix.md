# Worker Backlog And Validation Matrix

**Current State Snapshot (for fast rehydration; latest entries + this block; full table below is ownership + validation history)**

- Date: 2026-06-03 (agentic docs reassess complete + new-owner efficiency purge of historical bloat; see AUTONOMOUS for meta; product lanes unchanged; files/roles slimmed)
- Date: 2026-05-28
- Current product: Blueprint factory for peds_asthma_parent_anxiety (and ED seed). 

**Rebalanced emphasis (per 2026-06 assessment)**: Core generation from case spec (communicationProfile, escalation triggers, empathy/parent tags, objectives) -> actor turn timelines, dialogue policies, emotion transitions into runtime/review/persistence. Supporting: consumer for manual realism evidence (operator selectable attachments to generated sessions for review/QA); materialization for spec-driven assets. See AUTONOMOUS for full assessment and pivot recommendation. Focus remains gates false, M1, no overclaims.
- Worker ownership highlights (see rows below for details): Worker 9/7/11 owns UI-XR runtime evidence consumer, preflight, submit preview, readiness, review pipeline big effort, replay projections. Worker 11/7 owns materialization evidence (attachments, plans, manifests, worker surfaces, Admin/API handoff, publication). Worker 7/8/9 owns admin runtime selection, publication queue, review replay, faculty decision surfaces + summaries. Worker 4/2 scenario + publication coverage. Worker 11 asset ladders + humanoid gate.
- Last rows (from tail): UI-XR review pipeline big effort (source+tool+route+projections+panels), UI-XR readiness submit preview, preflight submit preview, explicit preflight output, artifact consistency, preflight validator, consumer projection actions, consumer action summary, etc. All carry "Next: continue deterministic runtime capture workflow..." or "pivot to ...".
- Next action: per blocker assessment + publish: Quest (only true hardware) does not block local gen lane (per AGENTS rec default continue local deterministic; addressing now = toil). Continue rebalanced post-publish: expand generator for fuller peds turns/emotion, wire caseDerived* to runtime-state. Record Per-Slice, focused verif, guards. See AUTONOMOUS.
- Agentic docs reassess (2026-06-03 meta): ./agent-factory/* + ./agents/* + supporting tools/agent-factory/* + docs/agent-factory/* now aligned to hyper OpenClaw (snapshots/Efficiency Quick Ref/lease/M1 Max 64GB/current UI-XR+materialization/product focus). No worker ownership change; used for lens on future slices. Guards green. See AUTONOMOUS meta record.
- 2026-06-03 Worker 9/11 UI-XR consumer operator selection: added operatorSelectable: true to submitPreview and operatorSubmissionWorkflow in consumer artifact + preflight (supports operator-selected payloads for guarded submit path). Verification: focused vitest -t "consumer" 6/6 pass; biome on ts. Next: continue consumer or materialization per snapshot.
- Rehydration: read AGENTS + PROJECT (snapshot+queue) + AUTONOMOUS (snapshot) + this file (this + recent rows). Then drift/lease. Update this matrix with new row for touched worker + verification + next. Alignment marker preserved: Worker 7 plus Worker 8 completed-station faculty review path; also note "Active Product Advancement Order" may be historical phrasing. Efficient Rehydration + Working Model applied here.

**Efficiency Quick Ref**: `tail -10 thisfile.md | grep -E 'Next|Worker'` for status. Always focused `vitest -t`. Lease before writes. Prefer `grep` tool. Snapshots first. Post-slice after every row added.

2026-05-27 OpenClaw hardening validation lane: long unattended runs must preserve `docs/openclinxr/openclaw-runbook-2026-05-27.md`, run `pnpm docs:drift-check` after cleanup or suspected drift, and keep each slice tied to a product path plus blueprint/factory contract. The Required Per-Slice Record belongs in `AUTONOMOUS_WORK_PLAN.md` or this backlog, not in scattered checkpoint/status files.

Required Per-Slice Record fields for OpenClaw-style operation: `Product path advanced`, `Blueprint/factory tie`, `Touched files`, `Evidence`, and `Next queued slice`. Use these fields here or in `AUTONOMOUS_WORK_PLAN.md` after each slice before queue transition.
PROJECT_COORDINATION_INDEX.md
Do not toil on evidence refreshes.

2026-05-28 Worker 7/9/11 Admin runtime evidence capture scaffold surface: API runtime-selection review and scene-generation publication readiness now attach the file-backed pediatric `runtimeEvidenceCaptureScaffold`, and Admin runtime-selection/environment review surfaces render the candidate counts, sample refs/paths, submit-candidate count, claim boundary, and false provider/runtime/learner/Quest gates. This makes the generated scaffold visible to reviewers before submission while reusing the existing runtime visual evidence attachment route and preserving no-readiness boundaries. Verification: Biome on touched API/Admin files; focused API runtime visual evidence/runtime-selection tests; focused and full Admin runtime-selection/environment queue tests; focused API-client publication/runtime visual evidence test. Next queued slice: consume the scaffold submit payload through the existing Admin submission handler or add a focused API acceptance fixture that proves scaffold candidates attach only after reviewed metadata-only decisions.

2026-05-28 Worker 9/11 runtime evidence capture scaffold artifact: Added `runtime:evidence-capture-scaffold` to turn the pediatric runtime-realism evidence input draft into deterministic metadata-only attachment candidates for the existing runtime visual evidence attachment route. The generated scaffold carries 3 runtime-realism candidates, 9 visual-QA candidates, and a `submitRuntimeVisualEvidenceAttachmentInput` payload while keeping provider execution, runtime execution, learner launch, Quest refresh, production asset readiness, clinical validity, and scoring validity false/not-evidence. Verification: focused scaffold unit test; Biome on new tool/test/package script; generated and validated `docs/openclinxr/encounter-runtime-evidence-capture-scaffold-peds-asthma-parent-anxiety-2026-05-28.json`; node assertion confirmed counts and false gates; focused API runtime visual evidence attachment tests; `pnpm docs:artifacts`. Next queued slice: consume the scaffold artifact in Admin/environment review or a narrow API fixture so reviewer submission can use the existing attachment-record route without runtime/Quest/learner readiness claims.

2026-05-28 Worker 9/11 UI-XR runtime evidence capture scaffold: UI-XR copied manual performance evidence now includes a metadata-only `runtimeVisualEvidenceCaptureScaffold` with attachment candidates compatible with the existing runtime-realism/visual-QA evidence-ref attachment record shape. The scaffold derives active actor runtime-realism and visual-QA candidate refs from the selected scenario/runtime evidence payload while keeping provider execution, runtime execution, learner launch, Quest refresh, production asset readiness, clinical validity, and scoring validity false/not-evidence. Verification: Biome on `apps/ui-xr/src/runtime-state.ts` and `apps/ui-xr/src/runtime-state.test.ts`; focused `@openclinxr/ui-xr` runtime-state tests; focused `@openclinxr/api` runtime visual evidence attachment tests. Nonblocking verification note: full `@openclinxr/ui-xr` typecheck still fails on existing broad strict-index-signature/fixture issues outside this slice; keep focused tests plus Biome as this slice gate until a dedicated typecheck cleanup slice. Next queued slice: either add a file-backed deterministic capture-scaffold artifact for downstream consumers or surface UI-XR scaffold candidates in Admin/environment review while all launch gates remain blocked.

2026-05-28 OpenClaw continuity lease validation lane: Added `pnpm openclaw:lease` as a local ignored-file lease for recurring heartbeat workers. Product path advanced: autonomous execution reliability for active factory slices. Blueprint/factory tie: heartbeats can now detect another active run before editing materialization, runtime, Admin, or factory artifacts, and can recover only after lease expiry plus `git status --short` inspection. Verification: `pnpm exec vitest run tools/openclinxr/openclaw-automation-lease.test.ts`; `pnpm exec biome check tools/openclinxr/openclaw-automation-lease.ts tools/openclinxr/openclaw-automation-lease.test.ts package.json`; CLI status/acquire/release smoke. Cadence: Codex heartbeat automation `openclinxr-openclaw-autonomous-continuation` now runs every 10 minutes. Next queued slice: Worker 11 actor-specific humanoid/equipment materialization inputs after `pnpm docs:drift-check` and `pnpm openclaw:post-slice`.

2026-05-28 Worker 11 materialization evidence attachment handoff: Publication now accepts `--materialization-evidence-report` and carries blocked actor/equipment-specific evidence refs into `actorEquipmentMaterializationGate.materializationEvidenceAttachment`; local launch selection and runtime-selection review packets preserve that attachment so Admin/runtime review surfaces can distinguish shared-neutral/generic contract blockers from per-actor/per-equipment missing evidence refs. Verification: focused publication/local-launch/runtime-selection tests; Biome on touched tools; regenerated and validated pediatric publication, local-launch, and runtime-selection artifacts; `pnpm docs:artifacts`. Boundary: evidence attachment remains blocked/non-attachable and does not claim runtime, Quest, production asset, clinical, scoring, or learner-launch readiness. Next queued slice: produce provider-neutral materialization input/work-order grouping for actor body/clothing/hair-face/rig refs and equipment mesh/scale/placement/affordance refs.

2026-05-28 Worker 11 materialization input manifest: Added `asset:encounter-materialization-input-manifest` to convert blocked materialization evidence refs into provider-neutral metadata-only work-order inputs. Pediatric asthma output groups 3 actor inputs and 6 equipment inputs, preserving body/clothing/hair-face/rig and mesh/scale/placement/affordance cue refs while blocking provider execution, paid APIs, external network, runtime, Quest, production, clinical, scoring, and learner-launch readiness claims. Verification: focused manifest test, Biome on tool/test/package script, generated and validated `docs/openclinxr/encounter-materialization-input-manifest-peds-asthma-parent-anxiety-2026-05-28.json`, `pnpm docs:artifacts`. Next queued slice: surface the input manifest in the asset-generation worker/Admin review queue as metadata-only tasks before any provider execution approval path.

2026-05-28 Worker 11 materialization input worker review surface: `asset:encounter-worker:run` now accepts `--materialization-input-manifest` and emits `materializationInputManifestSummary` in worker reports. The pediatric worker report preserves 3 actor and 6 equipment provider-neutral inputs, required cue IDs, all blockers, and false provider/paid/external-network boundaries. Verification: focused worker tests; Biome on worker tool/test; generated and validated `docs/openclinxr/encounter-asset-generation-worker-peds-asthma-parent-anxiety-2026-05-28.json`; `pnpm docs:artifacts`. Next queued slice: expose this worker summary in Admin route/client UI review surfaces so reviewers can inspect the planned materialization inputs without raw artifact inspection.

2026-05-28 Worker 11/7 Admin materialization input summary surface: Runtime-selection review now exposes the worker report's `materializationInputManifestSummary` through the API/client/Admin panel. Reviewers can see 3 actor and 6 equipment provider-neutral materialization inputs, actor/equipment cue groups, blockers, and explicit false provider/paid/external-network execution boundaries without opening raw JSON; runtime selection, provider execution, Quest readiness, production asset readiness, clinical validity, scoring validity, and learner-launch readiness remain blocked. Verification: Biome on touched API/Admin files; focused UI-admin runtime-selection panel and API-client tests; focused API runtime-selection review packet route test. Next queued slice: add the same bounded materialization input summary to the scene-generation publication readiness/3D Environment Generation Queue path so Admin review sees the work-order input plan before local XR handoff too.

2026-05-28 Worker 11/7 publication-readiness materialization input summary surface: Scene-generation publication readiness now attaches the worker `materializationInputManifestSummary` for the pediatric scenario and the 3D Environment Generation Queue displays the metadata-only actor/equipment materialization input plan before local XR handoff. The surface preserves actor/equipment cue groups, blocker count, and false provider/paid/external-network flags without clearing runtime selection, provider execution, Quest readiness, production asset readiness, clinical validity, scoring validity, or learner-launch readiness. Verification: Biome on touched API/Admin files; focused Environment Generation Queue and API-client tests; focused API draft pediatric publication-readiness test. Next queued slice: add a metadata-only Admin/API review-action packet for actor/equipment materialization inputs so reviewers can hold or mark inputs reviewed before any provider execution approval path exists.

2026-05-28 Worker 11/7 materialization input review action packet: Scene-generation publication readiness now derives `materializationInputReviewActionPacket` from the worker input summary and UI-admin displays review/hold actions for actor and equipment materialization inputs. The packet keeps provider execution, runtime execution, learner launch, Quest evidence refresh, production asset readiness, clinical validity, and scoring validity false/not-evidence while exposing input counts, blocker counts, and cue IDs for reviewers. Verification: Biome on touched API/Admin files; focused Environment Generation Queue and API-client tests; focused API draft pediatric publication-readiness test. Next queued slice: persist or replay metadata-only selected reviewer hold/review decisions for these materialization inputs across publication-readiness refreshes without adding provider execution approval.

2026-05-28 Worker 11/7 materialization input review decision record: Added a metadata-only REST/API/UI-admin client path for posting materialization input review decisions on scene-generation requests. Publication readiness now preserves a `materializationInputReviewDecisionRecord` with reviewed/held counts across refreshes, and the 3D Environment Generation Queue displays the decision posture without creating any provider execution, runtime execution, learner launch, Quest evidence, production asset, clinical validity, or scoring-validity claim. Verification: Biome on touched REST/API/Admin files; focused Environment Generation Queue and API-client tests; focused API draft pediatric publication-readiness test; focused REST route contract test. Next queued slice: surface the same decision record at runtime-selection review/local XR handoff so reviewer hold/review posture remains visible when runtime selection stays blocked.

2026-05-28 Worker 11/7 runtime-selection materialization input decision handoff: Runtime-selection review packets now merge the selected scenario's metadata-only `materializationInputReviewDecisionRecord`, and UI-admin renders reviewed/held actor/equipment input counts plus decision details at the local XR handoff boundary. The record preserves provider/runtime/learner/Quest execution false flags and does not clear materialization, production asset, clinical, scoring, or learner-launch gates. Verification: Biome on touched API/Admin files; focused Runtime Selection Review Packet panel and API-client tests; focused API runtime-selection decision propagation test. Next queued slice: add a narrow full Admin route smoke or fixture handoff for this decision record, then continue Worker 11 actor/equipment materialization evidence attachment work.

2026-05-28 Worker 7/11 Admin route materialization decision smoke: The full `/exam-forms` Admin route fixture now carries `materializationInputReviewDecisionRecord` through the runtime-selection review packet and asserts reviewed/held actor/equipment materialization input posture remains visible at the local XR handoff. The smoke keeps provider execution, runtime execution, learner launch, Quest evidence refresh, production asset readiness, clinical validity, and scoring validity false/not-evidence. Verification: focused App route smoke and Biome on touched Admin/API files. Next queued slice: continue Worker 11 actor/equipment materialization evidence attachment work by adding the next reusable evidence input/attachment seam without provider execution.

2026-05-28 Worker 11 materialization attachment plan seam: Added `asset:encounter-materialization-attachment-plan` to convert provider-neutral actor/equipment materialization inputs into reusable missing-evidence attachment slots. The pediatric plan creates 12 actor attachment slots and 24 equipment attachment slots for actor-specific humanoid GLBs/review/visual-QA refs and equipment-specific GLB-or-prefab/scale-placement/affordance refs while provider execution, runtime selection, learner launch, Quest refresh, production asset readiness, clinical validity, and scoring validity remain false/not-evidence. Verification: focused attachment-plan test, Biome on tool/test/package script, generated and validated `docs/openclinxr/encounter-materialization-attachment-plan-peds-asthma-parent-anxiety-2026-05-28.json`, `pnpm docs:artifacts`. Next queued slice: surface the attachment plan summary in worker/Admin/runtime-selection review surfaces before any provider execution path exists.

2026-05-28 Worker 11 materialization attachment plan worker surface: `asset:encounter-worker:run` now accepts `--materialization-attachment-plan` and emits `materializationAttachmentPlanSummary` in worker reports. The pediatric worker report exposes 12 actor slots, 24 equipment slots, 36 missing attachment slots, actor/equipment cue groups, blockers, and false provider/runtime/learner/Quest boundaries without raw JSON inspection or provider execution. Verification: focused worker and attachment-plan tests; Biome on touched worker/attachment tool files; regenerated and validated `docs/openclinxr/encounter-asset-generation-worker-peds-asthma-parent-anxiety-2026-05-28.json`; `pnpm docs:artifacts`. Next queued slice: surface the attachment plan summary in API/Admin runtime-selection review or publication-readiness panels.


--- HISTORICAL ROWS PURGED (new owner 2026-06 efficiency) ---
Old 100+ validation rows (pre 05-28 detailed) removed. See .bak or git for full. Use snapshot + tail -50 | grep -E "Worker|Next|2026-06" . Snapshots are truth.

2026-06-03 New owner purge (meta): Historical bloat purged (70->9 agents, 39M->6M artifacts, ledgers slimmed). Core workers unchanged. See AUTONOMOUS. Guards green.
- 2026-06-03 Worker 9/11 UI-XR consumer selectable count: exposed operatorSelectableAttachmentCount + enabled in preflight/artifact for operator-selected payload support. Focused test 6/6. Next per snapshot.
- 2026-06-03 Content slop review (meta): trimmed verbose purge notes, long Working model dupe, ugly proposal historical repeats, charter marker junk + filler. Aligned to lean model. See AUTONOMOUS. Guards green.
- 2026-06-03 Structure reorg (meta): tools/openclinxr/ flat 186 -> factory/ (41 core for product/UI-XR/materialization), openclaw/ (automation), evidence/ (143 support). .openclinxr/README + madr->docs/madr. Imports/scripts fixed. Lowers agent load for slices. See AUTONOMOUS. Guards green.
- 2026-06-03 UI-XR consumer (product): added operatorSelectionSupport in factory/ui-xr-runtime-evidence-consumer.ts (post reorg split). Test in factory/ sub 6/6. Next per snapshot.
- 2026-06-03 Tech gaps/strategies for plan (meta + product, Worker 9/11 + core): gaps (orchestrator, emotion FSM, runtime executor, gates, persist timelines, speech integration, input handler, retarget driver); strategies (discovery via targeted+agents+MADRs+spikes, vet 5Qs/rubric/OSS, reassess drift/alignment/anti-toil). Enhanced derive+emotionTimeline. Next per state. See AUTONOMOUS.
- 2026-06-03 Codebase one-shot fit (meta) + Admin UI-XR operator selection surface (product, Worker 9/11): meta quantified 268 core src files / ~134k ln active (factory/ + 9 agents + openclaw + agent tools + pkgs/apps src + key md) now lean/purposeful for efficient one-shot reasoning on full system (factory drives peds case to runtime/review, OpenClaw daily, snapshots rehydrate, gates false, M1 primary). Product: extended operatorSelectableAttachmentCount/Enabled/Support:'subset-via-count' (from consumer preflight/submitPreview) into API type+buildUiXr..., api-client, ReviewReplayReadinessSummaryPanel (renders "operator-selectable up to N" metric + support type) + tests. Verification: biome clean; consumer 6/6; api reviewReplay/ui-xr focused 92 pass; panel 1/1. Blueprint tie: makes operator-selected UI-XR payloads visible in Admin review summary (review-safe metadata, raw hidden, no gates cleared). Next per AUTONOMOUS/PROJECT: continue wiring or materialization.
- 2026-06-03 Publish all changes to main+github (meta, explicit request): 1259 files (reorg by 6 purposes, purge bloat historical, rebalance gen primary from peds spec with derive turns/emotion in packet + consumer operator support, tech strategies, blocker No, doc succinct, validate green) committed + pushed (fe58d65, origin main). Guards pre-push green. Lease (grok). Blueprint tie: durable published state for autonomous continuation on rebalanced factory direction. Next: expand peds generator + wire to runtime-state per snapshot.
- 2026-06-03 Worker 9/11 expand peds turn generator + caseDerived wire to ui-xr (product): derive now richer (full tags + 3-actor profiles -> turns/emotionTimeline with esc/deesc transitions + hints); caseDerivedEmotionSeed wired to RuntimeVisualEvidenceCaptureScaffold (type + build + comment + test). Verification: biome; packet derive tests 4/4 (rich peds + null); ui-xr seed+scaffold 2/2; post guards (26 align, 139/413 drift). Next per snapshot: emotion/dialogue policy or materialization.
