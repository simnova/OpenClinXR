I’ll check the current clause (3), manifest seed, session-id matcher, and the three create payloads.**READY FOR PREPARED IDLE CARD + RECEIVING AGENT INGESTION.** Not Plant, not dispatch.

The three create payloads are identical (`parentId tsk_e8ac84dd9cb6ac06`). Clause (3) now requires this exact current-prose sentence in one-line + do-this:

`The doc-authority-registry-2026-05-27.json takes precedence over this ENTRYPOINT’s “Read this first” and historical “AUTHORITATIVE” wording.`

Seed `expectedBeforeEntrypointEdit` names clauses (2)–(3) including that notice, clause (7) while review is `pending-owner-review`, and `5 ordinary passes plus 3 expected failures`. Clause (7) matches native ids with `/^[A-Za-z0-9][A-Za-z0-9_.:-]{7,199}$/` (UUID or other stable native forms), requires distinct worker/reviewer ids, `status: "accepted"`, review record, timestamp, and live hashes of ENTRYPOINT + ledger md/json + proposal. Labels still do not authenticate or grade quality.

Copy, commit, create Idle from that payload, read `doneWhen` back, unflip (2)(3)(7) in a throwaway copy, then the owner may Plant.