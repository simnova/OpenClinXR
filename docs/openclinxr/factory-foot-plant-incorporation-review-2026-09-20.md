# Factory foot-plant incorporation — five-turn adversarial review

2026-09-20. Consultation only: no card planted, no board mutation, no product implementation.

Five completed exchanges with `implementation-plan-gap-attacker` (spawned as `general-purpose` / grok-4.6 after typed `explore` spawn failed with `missing field created`). One parallel curious-researcher scout (same model). Parent session: Grok 4.6 orchestrator. Tree at review: `cd93026bf`.

Authority: consultation record. `agentInstructionWeight: none`. Do not treat this file as a queue.

## BLUF

Plant is runtime (`applyStanceLockedGroundAdvance` XZ pin + `solveTwoBoneIK`). SC-00 forbade a different clip as the plant remedy.

Factory next is not plant. Mesh2Motion `Walk_Formal` is in-place (`extras {}`, root net 0, no `Walk_Formal_RM`); graft discarded nothing.

Do not plant `factory-motion-retarget-cc0-default-and-flags` as foot-plant progress: `factory:case --motion-bind` is default OFF; dark-factory chain has no `motion_retarget`; no `pnpm asset:motion-bind`.

First product slice: `clip-facing-rebind-walk-formal` (Q1 heading). BOD gate: rewrites shipped physician clip bytes; no autonomous Blender.

Website: heading only. Never “factory planted feet.”

Refuse: IK bake, extras schema, FLOOR constraints, Holden/PBD, `Walk_Formal_RM`, `bound-clip-foot-plant.json` as product, `walk_cycle_from_dict`, `materialize_mpfb_humanoid_candidate.py` (#576). Seated-rest last-step stays.

claimScope: sequencing judgment from tree probes + SC-00/SC-04/SC-05 retained evidence.
notEvidenceFor: factory foot-planting landed, gait realism, Quest, browser A08, `runtime_playback` of a baked plant.

## What is already true

| surface | job | plant? |
|---|---|---|
| `stance-lock-mod.ts` `applyStanceLockedGroundAdvance` | XZ-pin stance toe; two-bone IK for Y | **runtime lock** (wired) |
| `case-owned-approach-runtime-mod.ts` | calls lock while `locomotion > 0` | consumer |
| `apps/ui-xr/src/main.ts` ~3494 | `applyStationBedsideStanceLock` **after** mixer pose | production path |
| `graft-bound-clip.ts` | copies Walk_Formal channels onto physician | no; provenance sidecar only |
| `motion_bind_stage.py` / `motion_retarget` | `mcp.load_and_retarget` | no; catalog station, not in dark-factory chain |
| `extract-library-clip.ts` | isolates one clip from 87-clip Mesh2Motion GLB | no; reports `rootTranslationNetMeters` |
| `materialize_mpfb_humanoid_candidate.py` | body/garment bake; seated-rest last-step on named stems | no walk plant; refuses CMU restore on those stems |
| `HumanService.create_human(feet_on_ground=True)` | rest-mesh on z=0 | already called from `fit_stage.py:565-569`; not clip plant |
| `bound-clip-foot-plant.ts` | **measures** slide | instrument, not baker |
| catalog `staging.plantOffsetMeters` | actor placement offset | name collision only |

Shipped physician provenance (`mpfb-clinical-physician-adult.provenance.json`): clip `openclinxr_retarget_walk_formal_cc0`, `inPlace: true`, `rootTravelMeters: 0`, `footPlantEvidence` → `sc-04.json`, `notEvidenceFor` includes `runtime_playback` and `visual_walk_quality`.

SC-00: Walk_Formal fails foot-slide 8.0× / 20.0× vs 0.005 m. Retired CMU also failed (3.6×). Recorded remedy: stance constraint, not a different clip.

## Turn log

| turn | id | result |
|---|---|---|
| T1 premise attack | `01a0c16b-721e-71a2-a6c1-5c128ca1eaf4` | Bake IK into Walk_Formal is the wrong next incorporation. Runtime lock landed. |
| T2 treatments table | `01a0c16f-f703-7a43-8c4f-7c9eebcad0a3` | No row is factory plant that clears D1/D9/Q1/Q5/inPlace/¬dup-lock/CC0. C′ land-path is the only honest factory row, still not plant. |
| T3 peer contradictions | `01a0c173-0b97-74e2-aab2-d3c61096ffb7` | −Z travel ≠ missing `--source-orientation`. C′ without a new bake is Q5/D9 hygiene. |
| T4 sequencing | `01a0c178-186d-7160-a54a-c1a5de0fcb6c` | Two slices; first CLI flags, no Blender; facing is BOD-gated Q1. |
| T5 skeptic | `01a0c17a-d558-74a0-bb40-df9da7ee650c` | **Overturn T4 slice 1** as vacuous for plant/Q1. Substrate override does not fire. Product next = facing, BOD. |
| researcher (parallel) | `01a0c16b-721e-71a2-a6c1-5c2ea910a6bb` | No factory plant API. Walk_Formal extras `{}`, no RM twin. Orange Duck MIT, unvendored. |

Typed `implementation-plan-gap-attacker` / `explore` spawns died (~53 s) with `serialization error: missing field created`. Escalation to grok-4.6 `general-purpose` succeeded.

## Treatments (success = every column)

D1 proven tool · D9 deterministic · Q1 visible · Q5 measurable · does not fight inPlace graft · does not duplicate lock · CC0/CC-BY.

| id | treatment | plant? | all columns? | disposition |
|---|---|---|---|---|
| A | 180° facing rebind of Walk_Formal | no (heading) | fails plant | **BOD-gated Q1** if asset rewrite allowed |
| B | glTF contact extras for the lock | labels only | fail (no schema; tautology if from 0.06 m band) | refuse |
| C | wire `motion_retarget` as written | no | fail (CMU default, extraArgs `[]`, default OFF) | refuse as-is |
| C′ | CC0 Walk_Formal + map + orientation + graft into dark factory | no | fails plant; Q5/D9 only if something actually calls it | **not a plant card**; optional later |
| D | Holden/Orange Duck offline PBD | would rewrite keys | fail this walk (dup lock; no source labels) | refuse until D14 preconditions |
| E | bake two-bone IK into hip/knee | yes, later | fail now (lock already zeros walk slide) | refuse until lock-less consumer |
| F | refresh `bound-clip-foot-plant.json` | no | anti-toil | refuse as product |

## Claim 7 (facing vs orientation) — measured T3

Live FK via `boundClipJointTrack` on `apps/ui-xr/public/generated-humanoids/mpfb-clinical-physician-adult.glb`.

| | missing `--source-orientation` | shipped Walk_Formal |
|---|---|---|
| feet Y | 0.72–1.31 m, legs back/up (`sc-04.md:122-134`) | toeL y 0.0006–0.303; toeR y −0.00098–0.302 |
| on floor | no | yes; SC-00 penetration 0.000984 m |
| rest toe−ankle | n/a | **+Z 0.128 m** |
| stance forward | n/a | **{x: −0.0122, z: −0.9999}** |
| root net XZ | n/a | **0** (in-place) |

These are two defects. `"Third person, Z up"` is the +90° X bind fix. It is not a 180° yaw fix. Seated parent bound without the flag is the orientation leftover (`sc-04.md:136-138`). Runtime `travelYawForClipForward` already yaws the slot to measured −Z so plant stays correct and the physician faces back down the route (`sc-05.md:365-374`).

Library inspect (`~/.openclinxr-tools/mesh2motion-app/static/animations/human-base-animations.glb` `Walk_Formal`): extras `{}`, root translation 2 keys net 0, no `Walk_Formal_RM`. RM-suffixed clips in that GLB are climb/hit/roll/shield/sword only.

## Failed treatments (freeze into any later brief)

| treatment | produced | refuse because |
|---|---|---|
| Bind glTF clip without `--source-orientation` | feet Y 0.72–1.31 m | +90° X from addon glTF→temp-BVH |
| `"Third person, Z up"` as 180° yaw | would re-apply X-up axes | wrong defect; shipped clip already on floor |
| Always-on `--source-orientation` on `runMotionBindOnce` | would hit #547 CMU BVH bind | flag is for glTF clips (`motion_bind_stage.py:59-71`) |
| `export_yup=True` + rest-data bake | mesh height on Z | #67 standing-export trap |
| CMU `02_01` / `07_01` in public GLB | CONDITIONAL redistribution | SC-04 |
| Mesh2Motion `Walk` instead of `Walk_Formal` | worse plant (0.54 vs 0.25 slide share) | `sc-04.md:98-110` |
| Bind stacked library without `extractLibraryClip` | binds `Chest_Open` | 87-clip GLB |
| Mesh2Motion without `--source-map` | fraction of chain | `motion_bind_stage.py:49-55` |
| `motionRetargetRunner.run` `extraArgs: []` | spawn with no `--actor/--clip` | `run.ts:53` |
| `factory:case --motion-bind` as written | CMU 07_01, no map/orient, `candidates/*.motion-bind.glb` | trap, not plant |
| Bake IK / extras onto Walk_Formal | duplicates lock; extras `{}` | SC-00 remedy is stance constraint |
| `bound-clip-foot-plant.ts` default clip | grades retired CMU | Walk_Formal in-place divides by 0 |
| `AnimationService.walk_cycle_from_dict` | AttributeError | UI calls it; **not defined** |
| MPFB `feet_on_ground=True` as clip plant | rest mesh on floor | already called |
| retarget_bvh mute/unmute IK as plant | mutes Rigify/MHX IK | not ground contact |
| bpy FLOOR / hand-authored PBD | n/a | D1 |
| Invent `Walk_Formal_RM` | absent in library | measured |

## D1 — wire, do not hand-author

Exists and must be reused: `extractLibraryClip`, `runMotionRetarget` / `motion_bind_stage.py`, `graftBoundClip`, `boundClipJointTrack` + `measureStanceGroundAdvance`, `solveTwoBoneIK`, `HumanService.create_human(feet_on_ground=True)`, `mcp.load_and_retarget`.

Do not hand-author: contact extras schema, FLOOR/IK bpy constraints, PBD, `Walk_Formal_RM`, velocity classifier.

Orange Duck [foot-locking](https://theorangeduck.com/page/inverse-kinematics-foot-locking) / [GenoView-InverseKinematics](https://github.com/orangeduck/GenoView-InverseKinematics) is MIT **code**, unvendored. Demo motion in that repo is not cleared. In-tree analogue is `solveTwoBoneIK`. Article contact annotation uses toe **velocity**; in-tree lock uses height band 0.06 m. PBD numbers are not in this tree (UNKNOWN).

retarget_bvh is GPL-2.0-or-later; already build-time only. MPFB addon is GPL-3.0-or-later; do not redistribute walkcycle JSON as a clip. Mesh2Motion clips: CC0 1.0 (`LICENSE-CC0.MD`); CarnegieMellonAnimations subtree excluded.

## Recommended next (after T5)

### Product: `clip-facing-rebind-walk-formal` (Q1 heading) — BOD gate

Physician face along the route. Not plant. Runtime already compensates.

BOD must approve rewrite of `apps/ui-xr/public/generated-humanoids/mpfb-clinical-physician-adult.glb` animation bytes. Operator standing order: no autonomous Blender bake. Prefer Node graft of existing channels (`graft-bound-clip.ts`). If BOD refuses the rewrite, do not plant; leave `travelYawForClipForward`.

writeRoots (draft): `tools/openclinxr/factory/graft-bound-clip.ts`, the physician GLB, its provenance JSON. Not `materialize_mpfb_humanoid_candidate.py`. Not motion-bind-cli.

knownGood (measured T3): rest `toe1-1.L − foot.L` z = +0.128 m; stance forward `{x: −0.0122, z: −0.9999}`. Treatment keeps rest +Z and flips stance `forward.z` to the same sign as rest toe−ankle z. Do not cite 0.005 m foot-slide; lock owns that.

Existing instruments: `the-shipped-humanoids-hash-to-their-provenance.test.ts`, `the-normal-encounter-physician-approaches-and-stops.test.ts`, `boundClipJointTrack`.

Website/progress.html allowed: “clip travel aligned to rig +Z (heading).” Forbidden: “foot lock baked,” “factory plant,” “SC-00 slide closed by bake.”

owner: asset-pipeline-lead (graft) + xr-systems-architect (heading regression).

### Not a plant card (optional later, not next)

CLI defaults off CMU + pass `--source-map` / `--source-orientation` on the **opt-in** bind. Helps the next human who passes `--motion-bind`. Does not reach `generate-mpfb-bone-map-coverage.ts` (hardcodes `cmu_07_01_walk.bvh`). Does not enter `DARK_FACTORY_CHAIN_STATIONS`. Anti-toil if sold as foot-planting progress. Substrate override does not apply: shipped walk land path is already the CC0 graft.

Adding `motion_retarget` to the dark-factory chain is a separate BOD/Blender item (`dark-factory-multi-case.test.ts:81` pins the chain).

## When a factory plant station becomes honest

All three:

1. D14 **generated** clip (not in-place Walk_Formal with extras `{}`).
2. Consumer **skips** the bedside lock (Model Vetting / bake vertical), so lock does not already zero walk slide.
3. **Source contact labels** exist. Height-band labels tautologize the lock. Velocity PBD is not an in-tree proven tool.

Until then refuse IK-baked keys, extras schema, FLOOR constraints, Holden import.

## Collision / holds

- `#576` holds `materialize_mpfb_humanoid_candidate.py`. Leave `SEATED_REST_OUTPUT_STEMS` + seated last-step.
- `docs/openclinxr/evidence/bound-clip-foot-plant.json` still names retired `openclinxr_retarget_cmu_02_01_walk`. Outside any plant card.
- 2026-09-12 runtime follow-up remains `docs/openclinxr/scene-closure-2026-09-09/foot-lock-ik-review-2026-09-12.md` (terminal turn). This review does not replace it.

## Residuals

- Learner-visible −Z heading until BOD facing rewrite.
- Browser A08 foot-slide still `not_gradeable` (~8 Hz).
- Terminal turn still misses plant (runtime follow-up, not factory).
- Opt-in bind still defaults CMU if someone passes `--motion-bind`.
- Dark factory still has no `motion_retarget` station.

## NOT TESTED

Live `factory:case --motion-bind` on current main. Node 180° Y channel rewrite preserving inPlace + floor Y. Model Vetting playback of Walk_Formal without the bedside lock. Holden article velocity numbers against MPFB `toe1-1.*`. Whether BOD will allow physician GLB rewrite.

Resume attacker: `resume_from=01a0c17a-d558-74a0-bb40-df9da7ee650c`. Resume researcher: `resume_from=01a0c16b-721e-71a2-a6c1-5c2ea910a6bb`.
