# Preparation review record

Scope: planning and board preparation only. Baseline `bb49e9675cba1f7edfe0f0fd80f798120f5bd46d`. No implementation worker was released. The specialist consultations were read-only; source/commit inspection does not establish a working prototype.

## Reviews completed

- Implementation-planning/XR specialist: traced authored-first catalogue, synthetic case identity, normal handoff, entry ownership and event acknowledgment. Recommended one persisted synthetic encounter instead of combining an ED supine scene with a separate Ward walking capture. Identified the review-only blocked handoff and server-only hashing/browser boundary.
- Asset/publication specialist: identified existing `docs/index.html` website, public Pages deployment of all docs, exact-source versus rendered-output rights, selected-asset provenance and durable source-video lineage. Recommended no new site and no implicit publishing.
- Independent plan-gap attacker, two passes: added wrong-run/stale observation rejection, desired initial predicate versus learner goal distinction, post-entry invalidation/stop, independent rubric prerequisite and floor-relative contact/penetration controls. Second pass found asset-substitution ownership, storage/retrieval responsibility and ordinary relative-link issues; those were corrected. Recommendation to treat planting as harmless staging was NOT adopted: in this system planting can release work, so all cards stay Idle/backlog.
- Grok 4.6, original persistent conversation `0a7c4705-0b58-439b-b119-ac8dd1ed6db4`, round 12: inspected current code, earlier review and draft package. Confirmed runtime gaps and required a real parent closure contract, explicit admission owners, a new normal-workflow capture test instead of modifying the injected-drive acceptance, scoped rubric tests, exact selected pin list and durable source retrieval. Incorporated these corrections. Its suggested SC-01/SC-04 parallel start was rejected: selected assets must be fixed by SC-01 before SC-04. The dependency graph preserves that order.

## What changed because of review

SC-00 freezes independent numeric limits and broken-control expectations before SC-03/05 grading. SC-02 enforces real API/UI admission and effect acknowledgment while preserving the pure specification boundary. SC-03 carries exact mounted support observations and verifies an authored offset edit on the loaded body. SC-04 owns approved substitutions in the shared authoring manifest and recompiles through its supported producer. SC-05 binds plans to observed geometry and stops on invalidation. SC-07 creates a normal-workflow capture test and proves durable restricted retrieval before publication derivatives. SC-08 is existing-site integration, with public release separate. SC-09 owns whole-outcome audit. SC-10 is separate, after SC-09, and cannot disguise an unexecuted research hold as measured model failure.

## Evidence discipline

The previous code review ran 15 targeted tests successfully but reproduced the required-runtime-state promotion hole in a separate source probe. This is why baseline command success alone cannot close the new cards. The 8/13 hash observation concerned comparable GLB/provenance pairs, not every file or the selected future demonstration set. Rights and hash review must follow the selected assets, not trigger blanket regeneration.

Final review verdict, document checks and board read-back are recorded in the [package index](./index.md) and [board manifest](./board-manifest.md) after completion of preparation. No future implementation evidence is fabricated here.

## Final consultation and disposition

Grok round 13 ended normally in the same conversation (`grok-4.6-build` backend). It accepted creation as unplanted tasks subject to three concrete residuals: put the parent body in the task document, keep SC-10 a sibling without authority to rewrite SC-04 licence rows, and narrow SC-01 UI changes/SC-04 instrument roots. All were applied before board creation. SC-01 must not change supported placement; SC-04 cannot rewrite the injected-drive capture. The parent contract explicitly requires direct full-run/source/site evidence.

BothyBoard's typed task read-back exposes objective/proofs/scope and a canonical body generated from those fields, rather than preserving arbitrary submitted body text. Essential required behavior, counterweights and completion discipline are therefore also stored verbatim in each child's objective field, not left only in an arbitrary body heading. Final read-back checks that complete objective, dependencies, scopes, project and Idle/backlog state.

## Completed preparation checks

All twelve cards were created and read back through authenticated MCP with the direnv token. Full objectives (including required behavior and counterweights), canonical body requirements, scopes, dependencies, project fields and parent links matched. Every card remained Idle/backlog with no session, assignee agent or worktree. The parent has ten children and SC-10 is a sibling.

Document validation passed: Markdown reference suite (9 tests); coordination alignment (23 files); drift check (447 Markdown files, 345 generated artifacts); Pages synchronization and static wiring. The local contract commit also passed all nine standard pre-commit guardrail stages, including 89 architecture tests and the 10-test typecheck error-count ratchet. The ratchet does not mean the repository has a clean full typecheck. These are preparation checks, not proof of the future encounter.

No product code, new acceptance-test implementation, fabricated evidence, worker dispatch or public push was included. An owner-memory pulse produced during consultation was left outside the planning commits.
