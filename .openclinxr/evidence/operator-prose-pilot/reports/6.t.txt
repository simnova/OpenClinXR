## Routes in use today that nothing enforces

**`ox-alpha`** appears in zero executable files. Greps of `packages/openclinxr/agent-loop/src/` and `tools/openclinxr/openclaw/` returned no hits. The ladder lives only in `PROJECT_STATUS.md`, a memory file, and the body of issue 576, all prose. When it was passed to `dispatch()`, the #461 downgrade guard stayed silent because `ox-alpha` is absent from `MODEL_RANK`, so it passed by being unrecognized rather than approved. `orchestrator-dispatch-loop` says never hand-pick a model; it was hand-picked.

The codex review route is half-codified. `role-harness-policy.ts:151` carries a `codex` column, pinning `gpt-5.4-mini` / `gpt-5.4` / `gpt-5.5`, while the installed CLI runs `gpt-5.6-sol`, one generation stale. The review invocation bypassed that table, and no skill or rule mentions `codex exec`, so the route existed only in conversation.

## Routing by activity

| activity | routes to | codified as |
|---|---|---|
| product code slice | grok worker, isolated worktree | code: `dispatch-worker.ts`, `role-harness-policy.ts` |
| read-only scouting | `explore` subagent on flash | rule: `agents/rules/TIER_GROK.md` |
| direction, priority, visual verdicts | superagent / ox standing thread | skills: `autonomy-boundaries`, `delegator-comms` |
| does-this-exist search | grok deep-research scout | rule: `PROTO_CURIOUS_RESEARCHER.md` |
| periodic honest review | outside model, today codex | nothing |
| pixel grading | me, never delegated | skill: `pixel-grading` |
| plant a RED, write a contract | me | skills: `contract-design`, `orchestrator-dispatch-loop` |
| integrate to main | me, serial | skill: `board-conduit` |

## Binding strength

Code that fails closed binds without me: `role-harness-policy.ts:157-183` maps tier to model per harness, `resolveDispatchModel` throws on an unreasoned downgrade, `briefFromIssue` refuses an issue lacking a machine-checkable `done_when`, merge-kill refuses an unverified contract. Repo rules load every session and bind by persuasion. Skills bind only when their trigger fires. Memory pointers are weakest.

The enforced layer describes a DeepSeek-and-grok world; today's routing is ox-alpha and OpenAI, held in prose.

NEXT: on your word, queue a card adding `ox-alpha` to `MODEL_RANK` with your fallback order, refreshing the codex column to `gpt-5.6-sol`, both pinned by a test.