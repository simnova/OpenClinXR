Expression capability is baked on the assets and nothing drives it at runtime.

**The gap.** `scenario-runtime.ts` instantiates a per-actor `EmotionEngine` from `resolveCaseEmotionPolicy`, so case definitions already carry emotional state and transitions. `EmotionEngine` appears nowhere in `apps/ui-xr`. The 32 face-unit names appear only in provenance files, a resolver, a census tool and tests. Both ends exist; no code joins them. This is the same incomplete-loop shape the repo has hit six times: a mechanism built and never called.

NOT TESTED: whether the 32 face units deform visibly when driven. Nothing has ever set them, so their amplitude is unmeasured, and the census tool measures presence rather than motion.

Two open issues qualify the asset side. #316 records that 15 of the Anny rail's 25 morphs are whole-body deformations rather than face targets, so that rail's morph count is not equivalent to the MPFB rail's; #316 is still open and is part of why #652 wants seven of those retired. #651 is separate: face geometry is not driven by phenotype, so four humanoids spanning ages 29 to 58 share one face.

What is on the assets: every MPFB humanoid carries 47 morph targets, being 15 Oculus visemes (`viseme_aa` through `viseme_U`) and 32 MakeHuman face units (`eyebrows-left-inner-up`, `mouth-corner-puller`, `eye-left-closure`, `nose-compression`, `neck-platysma`). Those are the `faceunits01` names from the pack you linked, present on 11 shipped assets.

| channel | state | driver |
|---|---|---|
| visemes / lip-sync | driven | `viseme-runtime-wire.ts`, `viseme-timeline-drive.ts`, `dialogue-visemes.ts` |
| blink | driven | `blink-runtime-wire.ts`, 4300 ms deterministic clock into `eye-*-closure` |
| expression / emotion | not driven | none |

Lip-sync closed through #578, #582 and #608 (Rhubarb acquired, proven, consumed). Blink closed as #379.

NEXT: an emotion-to-face-unit map plus a runtime wire, mirroring `blink-runtime-wire.ts`.